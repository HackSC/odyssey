import styled from "styled-components";

const Body = styled.p`
  font-size: 16px;
  line-height: 24px;
  margin-bottom: 24px;
`;

const Bold = styled.b`
  font-weight: 600;
`;

const Italic = styled.em`
  font-style: italic;
`;

export default Body;
export { Bold, Italic };
